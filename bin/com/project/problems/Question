Class-> Question1.
   a.find the element which has highest frequency in array.
   b.remove all duplicate from array.
   c.Find all duplicate element in array.
Class -> Question2
    a.Find sequence in String

